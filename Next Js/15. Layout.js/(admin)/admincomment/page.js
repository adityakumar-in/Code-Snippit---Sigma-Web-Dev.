// Like if you don't want to access admincomment through /adimin route, so you can wrap admin file with (admin) so in this way you can dirrectly access admincomment, adiminlogin and so on
import React from 'react'

const page = () => {
  return (
    <div>
      admin comment
    </div>
  )
}

export default page
